# Artefatos de build (pygbag). 20 MB de .apk + .tar.gz estavam versionados
# junto com o projeto — são regeneráveis com `python -m pygbag main_web.py`.
build/
site/jogo_web/

# Preferências salvas em disco pelo próprio jogo (ver audio.py/save_settings
# e game.save/load) — são de cada máquina, não do projeto.
audio_settings.json
save.json

# Backups e fontes de arte
*.bak_v*
*.ase

__pycache__/
*.pyc
